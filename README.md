Please install uv first.
```curl -LsSf https://astral.sh/uv/install.sh | sh```

To get started, start by cloning the repository.
Then move to the nevus_app directory and start the app:
```
cd nevus_app
uv run app.py
```
Navigate to localhost:5000 in your browser and you can start submitting photos.
